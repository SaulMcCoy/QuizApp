<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header text-center">
                        <% if (typeof isGuest !== 'undefined' && isGuest) { %>
                            <h2>Welcome, Guest! 🎮</h2>
                            <p class="text-muted">Enjoy playing without creating an account!</p>
                        <% } else { %>
                            <h2>Welcome back, <%= username %>! 👋</h2>
                            <p class="text-muted">Ready to test your knowledge?</p>
                        <% } %>
                    </div>
                    <div class="card-body">
                        <div id="quiz-container">
                            <!-- Quiz content will be loaded here -->
                            <div class="text-center">
                                <h4>Quiz Loading...</h4>
                                <div class="spinner-border" role="status">
                                    <span class="sr-only">Loading...</span>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Score display -->
                        <div id="score-display" class="text-center mt-3" style="display: none;">
                            <h5>Your Score: <span id="current-score">0</span> / <span id="total-questions">10</span></h5>
                        </div>
                        
                        <!-- Navigation buttons -->
                        <div class="row mt-4">
                            <div class="col-6">
                                <button id="prev-btn" class="btn btn-secondary btn-block" disabled>Previous</button>
                            </div>
                            <div class="col-6">
                                <button id="next-btn" class="btn btn-primary btn-block">Next</button>
                            </div>
                        </div>
                        
                        <!-- Home button for guests -->
                        <% if (typeof isGuest !== 'undefined' && isGuest) { %>
                            <div class="text-center mt-3">
                                <a href="/signup" class="btn btn-outline-primary">Create Account to Save Progress</a>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Pass username and guest status to JavaScript -->
    <script>
        window.currentUser = {
            username: '<%= username %>',
            isGuest: <%= typeof isGuest !== 'undefined' ? isGuest : false %>
        };
    </script>
    
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/javascripts/script.js"></script>
</body>
</html>